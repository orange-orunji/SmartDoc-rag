# ═══════════════════════════════════════════════════════════
# 运行时依赖 —— API / Worker / Docker 部署必需
#
# 版本策略：~= 锁定小版本（以本地实测可运行版本为基线），
# 避免 LangChain 生态跨 minor 的破坏性变更；如需精确复现请
# 改用 pip freeze > requirements.lock。
#
# 开发/测试依赖见 requirements-dev.txt
# 可选 Streamlit 前端依赖见 requirements-ui.txt
# ═══════════════════════════════════════════════════════════

# ── Web 框架 ──────────────────────────────────────────────
fastapi~=0.139.0
uvicorn~=0.51.0
slowapi~=0.1.10
python-multipart~=0.0.29

# ── 数据模型 / 配置 ───────────────────────────────────────
pydantic~=2.13.4
pydantic-settings~=2.14.2   # python-dotenv 由其自动传递安装

# ── LLM / LangChain 生态 ──────────────────────────────────
openai~=2.45.0
langchain-core~=1.4.9
langchain-openai~=1.3.4
langchain-chroma~=1.1.0     # 自动传递安装 chromadb
langchain-text-splitters~=1.1.2
langgraph~=1.2.9            # 自动携带 prebuilt / checkpoint / sdk

# ── 检索（分词 / BM25）────────────────────────────────────
jieba~=0.42.1
rank-bm25~=0.2.2

# ── 重排序（本地 bge-reranker 推理）──────────────────────
# torch / transformers 由本包自动传递安装；Docker 中如需瘦身
# 可改用 CPU 版 PyTorch 索引安装
sentence-transformers~=5.5.1

# ── 语义缓存 / 向量运算 ──────────────────────────────────
numpy~=2.4.6

# ── 缓存 & 消息队列 ──────────────────────────────────────
redis~=8.0.1
aio-pika==9.4.3            # 与部署环境 RabbitMQ 兼容性验证过，勿随意升级

# ── 数据库 ORM ───────────────────────────────────────────
sqlalchemy~=2.0.50

# ── 文档解析 ─────────────────────────────────────────────
PyPDF2~=3.0.1
python-docx~=1.2.0

# ── 认证 / JWT ───────────────────────────────────────────
python-jose[cryptography]~=3.5.0
passlib~=1.7.4            # 密码哈希仅用 pbkdf2_sha256，无需 [bcrypt] extra
